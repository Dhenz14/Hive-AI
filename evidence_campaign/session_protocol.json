{
  "protocol_version": 2,
  "purpose": "Session-local baseline regime. The unit of evidence is the admitted session. No globally portable baseline exists.",

  "epistemology": {
    "claim_shape": "Within an admitted warm session, the frozen intervention produces paired pre/post improvement without holdout regression.",
    "not_claimed": "The system improved by X against a canonical baseline across the month.",
    "unit_of_analysis": "The admitted session, not the repo commit and not the design baseline.",
    "design_baseline_role": "Diagnostic artifact only (commit 4b4b083). Not used for pass/fail. Logged for drift monitoring."
  },

  "server_identity": {
    "model_hash": "6f96c8466758aca9e3a21dbcc6cafceaaf0cf3f8b1e09c871fdaafa432d2c27d",
    "model_file": "/opt/hiveai/project/models/deploy/current_base.gguf",
    "server_binary": "/opt/hiveai/llama-cpp-build/build/bin/llama-server",
    "startup_flags": "-m <model> --port 11435 -ngl 99 --ctx-size 4096 --flash-attn on -t 12",
    "scorer": "campaign_anchor_eval.py score_response (byte-identical to regression_eval.py)"
  },

  "warmup_procedure": {
    "step_1": "After server reports healthy (/health returns ok), run 3 throwaway generic inference calls (discarded). Flushes CUDA initialization.",
    "step_1b": "Run 1 throwaway round of all 5 anchor probes (results discarded). Primes KV cache with probe-like content.",
    "step_2": "Wait 3 seconds for GPU state to stabilize.",
    "step_3": "Run all 5 anchor probes with frozen parameters (temp=0, seed=42, top_k=1, max_tokens=2048).",
    "step_4": "Run the same 5 anchors a second time. If any score differs between step 3 and step 4, the session has not stabilized — repeat steps 1-4 up to 2 more times.",
    "step_5": "If step 4 scores match step 3, session is internally stable. Proceed to admission."
  },

  "admission_check": {
    "description": "Admission proves intra-session determinism. It does NOT compare to a fixed reference.",
    "anchor_probes": ["js-generics", "py-metaclass", "rs-ownership", "cpp-variadic", "rs-errors"],
    "hard_fail_rules": [
      "Stabilization failed after 3 warmup cycles → SESSION REJECTED. Server is non-deterministic.",
      "Any anchor returns empty response → SESSION REJECTED. Server malfunction."
    ],
    "if_rejected": "Do not proceed. Investigate server state, restart, retry.",
    "if_admitted": "The stabilized anchor scores become this session's local baseline. Proceed to full baseline capture."
  },

  "session_baseline_capture": {
    "description": "After admission, run ALL 40 campaign probes once. This is the session's pre-intervention baseline.",
    "probes": "All probes in campaign_full_baseline.py (JS=10, Python=10, Rust=10, C++=10).",
    "output": "session_baseline_{session_id}.json — frozen for this session.",
    "timing": "Must complete before any training attempt in this session.",
    "role_labels_computed": "weakness_type and holdout_role are computed from session-local scores, not from the design baseline."
  },

  "diagnostic_comparison": {
    "description": "After baseline capture, compare session-local scores to the design baseline (commit 4b4b083). This is informational only — it does not gate anything.",
    "design_baseline": {
      "B1_js_generics": 0.900,
      "B2_py_metaclass": 0.767,
      "B3_rs_ownership": 0.740,
      "B4_cpp_variadic": 0.925,
      "B5_rs_errors": 0.525
    },
    "logged_fields": ["per_anchor_deviation", "mean_deviation", "weakness_label_changes"],
    "action": "Log only. Do not reject session. Do not modify campaign design."
  },

  "non_anchor_sentinels": {
    "purpose": "Spot-check non-anchor fit probes after admission. Informational only.",
    "sentinel_probes": ["rs-patterns", "py-context-mgr", "cpp-raii", "js-proxy"],
    "action": "Log scores and deviations from design baseline. No gate effect."
  },

  "hard_rules": {
    "rule_1_session_container": "Each campaign run must have a session_id and a frozen session manifest. No result counts unless attached to an admitted session.",
    "rule_2_paired_deltas_only": "For every fit or holdout evaluation, compute effect against the pre-intervention baseline from the SAME admitted session. Never compare to the design baseline or any other session.",
    "rule_3_no_cross_session_raw_scores": "Raw scores across restarts are not comparable. Aggregate only on: closure/non-closure, signed delta, normalized headroom closed, regression/non-regression.",
    "rule_4_restart_new_epoch": "Any restart opens a new measurement epoch. New admission, new baseline, new session_id. Never splice pre-restart and post-restart raw scores into one series."
  },

  "session_log_format": {
    "fields": [
      "session_id (UTC timestamp of admission)",
      "warmup_rounds (how many cycles needed for stability)",
      "anchor_scores (all 5, from stabilized run)",
      "anchor_weakness_types (computed from session-local scores)",
      "diagnostic_deviations (vs design baseline, informational only)",
      "sentinel_scores (4 probes, informational)",
      "admission_result (admitted / rejected)",
      "full_baseline_file (path to session_baseline_{id}.json)"
    ]
  },

  "campaign_limitation": {
    "statement": "The measurement instrument is session-dependent. Cold-start drift up to 0.116 was observed. Evidence is valid only as paired within-session deltas under a controlled admission protocol.",
    "containment": "Session-local baselines with explicit admission gate.",
    "post_campaign_target": "Eliminate session-dependent variance (deterministic CUDA mode, fixed KV cache init, warm-start checkpoint)."
  }
}
